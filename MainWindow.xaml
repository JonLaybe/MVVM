<Window x:Class="WpfApp22.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp22"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <local:ViewModel></local:ViewModel>
    </Window.DataContext>
    <StackPanel>
        <Label Content="{Binding Text2, UpdateSourceTrigger=PropertyChanged}" FontSize="25px"></Label>
        <TextBox Text="{Binding Text, UpdateSourceTrigger=PropertyChanged}" FontSize="25px"></TextBox>
        <Button Background="{Binding SolorColor, UpdateSourceTrigger=PropertyChanged}" Height="200px"/>
        <Slider Value="{Binding R, UpdateSourceTrigger=PropertyChanged}" Maximum="200"></Slider>
        <Slider Value="{Binding G, UpdateSourceTrigger=PropertyChanged}" Maximum="200"></Slider>
        <Slider Value="{Binding B, UpdateSourceTrigger=PropertyChanged}" Maximum="200"></Slider>
        <Button Command="{Binding ButtonClick}">Good day</Button>
        <Button Command="{Binding ButtonClick2}">Bed day</Button>
    </StackPanel>
</Window>
